--[[
	Generated by the Luna extension
	Last changed: (STARTDATE)
	Time taken: (TIME)
	Number of modules: (MODULES_AMOUNT)
]]

local modules = {}

coroutine.wrap(function()
	task.wait()

	--REQUIRE_HERE

	-- Call 'Init' then 'Start'
	local function traverse(array, funcName)
		for _, v in array do
			if typeof(v) == "table" then
				if v[funcName] then
					v[funcName]()
				end
				traverse(v)
			end
		end
	end
	traverse(modules, "Init")
	traverse(modules, "Start")
end)()

return modules
